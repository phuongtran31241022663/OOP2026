# Plan - Fix build errors and missing services

## Goal
Resolve all compilation errors and implement missing core services required for the project to build and run.

## Context
- `InMemoryDriverGrid` is missing but used in `MatchSvc` and `Simulation`.
- `RevSvc` is missing but used in `PsgSvc` and `Form1` (implied).
- `NotificationSvc` is missing but used in UI components.
- `IFareService` vs `IFareSvc` naming inconsistency.
- Property nullability warnings in `PhotonResponse` and `Addr`.

## Proposed Changes

### 1. Fix Interface Naming Inconsistency
- Rename `IFareService` to `IFareSvc` where used, or ensure both exist if they serve different purposes.
- Currently, `FareSvc` implements `IFareSvc` (defined in `Interface.cs`).
- Error in `PsgSvc` suggests it expects `IFareService` or similar. Actually, looking at `Service.cs`, `PsgSvc` constructor uses `IFareSvc`.
- Wait, the error `Argument 6: cannot convert from 'OOP2026.FareSvc' to 'IFareService'` means some class (likely `PsgSvc` or where it's instantiated) expects `IFareService`.
- I will check `Program.cs` or `Form1.cs` to see where services are instantiated.

### 2. Implement Missing Services in `Service.cs`

#### `InMemoryDriverGrid`
- Implement a spatial indexing grid to efficiently find nearby drivers.
- Methods needed: `UpdateDriverLocation`, `RemoveDriver`, `GetDriversInCellAndNeighbors`.

#### `NotificationSvc`
- Implement `INotificationSvc` to provide localized messages for status changes.

#### `RevSvc`
- Implement `IRevSvc` to handle creating/managing reviews.

### 3. Fix Property Nullability and Constructor warnings
- Add `required` modifier or initialize properties in `Addr` and `PhotonResponse` classes.

### 4. Implementation Steps

#### Phase 1: Diagnostics & Fix Naming
- [ ] Read `Program.cs` to see dependency injection/instantiation logic.
- [ ] Check for `IFareService` definition. If it's a duplicate or old name, consolidate to `IFareSvc`.

#### Phase 2: Implement Missing Core Logic
- [ ] Implement `InMemoryDriverGrid` in `Service.cs`.
- [ ] Implement `NotificationSvc` in `Service.cs`.
- [ ] Implement `RevSvc` in `Service.cs`.

#### Phase 3: Fix PhotonResponse models
- [ ] Find where `PhotonResponse` and `OsrmResponse` are defined (likely `Service.cs` or separate file).
- [ ] Fix the nullability warnings by providing default values or marking as nullable.

#### Phase 4: Final Cleanup & Build
- [ ] Run `dotnet build` to verify all errors are gone.

## Verification
- `dotnet build` must succeed with 0 errors and ideally 0 warnings.
- Check that `Simulation` and `MatchSvc` can correctly interact with `InMemoryDriverGrid`.
- Check that `PsgSvc` can correctly interact with `RevSvc`.
